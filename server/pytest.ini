[pytest]
minversion = 8.0
testpaths = tests
addopts = -ra -p no:pytest_asyncio

markers =
    unit: fast tests with fakes/mocks; no external services required
    integration: DB-backed tests; can touch postgres/redis; slower

filterwarnings =
    error::pytest.PytestUnknownMarkWarning