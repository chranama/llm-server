# deploy/compose/docker-compose.yml
#
# ==========================
# Templates / extension fields
# ==========================

x-server-base: &server_base
  build:
    context: ../..
    dockerfile: deploy/docker/Dockerfile.server
  restart: "no"
  environment:
    APP_ROOT: "/app"

    # Defaults (local/dev)
    ENV: ${ENV:-dev}
    MODEL_LOAD_MODE: ${MODEL_LOAD_MODE:-lazy}
    REQUIRE_MODEL_READY: ${REQUIRE_MODEL_READY:-0}

    # infra toggles
    REDIS_ENABLED: ${REDIS_ENABLED:-0}

    WORKERS: ${WORKERS:-1}
    UVICORN_RELOAD: ${UVICORN_RELOAD:-0}

    # Capabilities live in the models yaml now
    MODELS_YAML: ${MODELS_YAML:-/app/config/models.yaml}

    # Phase 0: canonical policy decision location inside container
    POLICY_DECISION_PATH: ${POLICY_DECISION_PATH:-/app/policy_out/latest.json}

  # IMPORTANT:
  # Dockerized server services are wired to the INTERNAL infra service names:
  #   postgres / redis
  # Host-wired infra uses postgres_host / redis_host and is for host-run server only.
  depends_on:
    postgres:
      condition: service_healthy
    redis:
      condition: service_started

x-postgres-base: &postgres_base
  image: postgres:16-alpine
  restart: unless-stopped
  environment:
    POSTGRES_USER: ${POSTGRES_USER:-llm}
    POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-llm}
    POSTGRES_DB: ${POSTGRES_DB:-llm}
  healthcheck:
    test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-llm} -d ${POSTGRES_DB:-llm}"]
    interval: 5s
    timeout: 3s
    retries: 10

x-redis-base: &redis_base
  image: redis:7-alpine
  restart: unless-stopped
  command: ["redis-server", "--save", "", "--appendonly", "no"]
  healthcheck:
    test: ["CMD-SHELL", "redis-cli ping | grep PONG"]
    interval: 5s
    timeout: 3s
    retries: 10

services:
  # ==========================
  # Data layer (INTERNAL-ONLY)
  # ==========================
  postgres:
    <<: *postgres_base
    profiles: ["infra"]
    expose:
      - "5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

  redis:
    <<: *redis_base
    profiles: ["infra"]
    expose:
      - "6379"
    volumes:
      - redis_data:/data

  # ==========================
  # Data layer (HOST-PUBLISHED)
  # For running llm_server on the host (not in docker).
  # NOTE: separate volumes so you can run infra + infra-host without collisions.
  # ==========================
  postgres_host:
    <<: *postgres_base
    profiles: ["infra-host"]
    ports:
      - "${POSTGRES_HOST_PORT:-5433}:5432"
    volumes:
      - pg_data_host:/var/lib/postgresql/data

  redis_host:
    <<: *redis_base
    profiles: ["infra-host"]
    ports:
      - "${REDIS_HOST_PORT:-6379}:6379"
    volumes:
      - redis_data_host:/data

  # ==========================
  # Integration test infra (ephemeral)
  # ==========================
  postgres_itest:
    image: postgres:16-alpine
    restart: "no"
    profiles: ["itest"]
    environment:
      POSTGRES_USER: ${ITEST_POSTGRES_USER:-llm}
      POSTGRES_PASSWORD: ${ITEST_POSTGRES_PASSWORD:-llm}
      POSTGRES_DB: ${ITEST_POSTGRES_DB:-llm_test}
    expose:
      - "5432"
    tmpfs:
      - /var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${ITEST_POSTGRES_USER:-llm} -d ${ITEST_POSTGRES_DB:-llm_test}"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis_itest:
    image: redis:7-alpine
    restart: "no"
    profiles: ["itest"]
    command: ["redis-server", "--save", "", "--appendonly", "no"]
    expose:
      - "6379"
    tmpfs:
      - /data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping | grep PONG"]
      interval: 5s
      timeout: 3s
      retries: 10

  # ==========================
  # Server (integration-test profile)
  # ==========================
  server_itest:
    <<: *server_base
    profiles: ["itest"]
    environment:
      ENV: test
      MODEL_LOAD_MODE: off
      REQUIRE_MODEL_READY: "0"

      # For integration tests that use DB wiring
      DATABASE_URL: ${ITEST_DATABASE_URL:-postgresql+asyncpg://llm:llm@postgres_itest:5432/llm_test}

      # Turn this on if you want cache paths exercised in itest
      REDIS_ENABLED: ${ITEST_REDIS_ENABLED:-1}
      REDIS_URL: ${ITEST_REDIS_URL:-redis://redis_itest:6379/0}

      # Optional: make it explicit that this should never warm models
      MODEL_WARMUP: "0"

      # In itest we still honor the canonical path, but allow override
      POLICY_DECISION_PATH: ${POLICY_DECISION_PATH:-/app/policy_out/latest.json}
    ports: []
    expose:
      - "8000"
    depends_on:
      postgres_itest:
        condition: service_healthy
      redis_itest:
        condition: service_healthy
    volumes:
      # allow tests to write/read artifacts deterministically if needed
      - ../../policy_out:/app/policy_out:rw

  # ==========================
  # Server (CPU / portable)
  # ==========================
  server:
    <<: *server_base
    profiles: ["server"]
    ports:
      - "${API_PORT:-8000}:8000"
    volumes:
      # Phase 0: canonical decision artifact is bind-mounted so policy can write and server can read
      - ../../policy_out:/app/policy_out:rw
      # Optional: bind-mount config for live edits
      # - ../../config:/app/config:ro
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8000/readyz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20

  # ==========================
  # Server (GPU)
  # ==========================
  server_gpu:
    <<: *server_base
    profiles: ["server-gpu"]
    environment:
      ENV: ${ENV:-prod}
      MODEL_LOAD_MODE: ${MODEL_LOAD_MODE:-eager}
      REQUIRE_MODEL_READY: ${REQUIRE_MODEL_READY:-1}
      MODEL_DEVICE: "gpu"
      REDIS_ENABLED: ${REDIS_ENABLED:-1}
      UVICORN_RELOAD: "0"
      MODELS_YAML: ${MODELS_YAML:-/app/config/models.yaml}
      POLICY_DECISION_PATH: ${POLICY_DECISION_PATH:-/app/policy_out/latest.json}
    ports: []
    expose:
      - "8000"
    volumes:
      - ${HOME}/.cache/huggingface:/root/.cache/huggingface
      - ../../policy_out:/app/policy_out:rw
      # Optional: bind-mount repo config (if you want live edits)
      # - ../../config:/app/config:ro
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8000/modelz || exit 1"]
      interval: 30s
      timeout: 10s
      start_period: 600s
      retries: 10
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: ["gpu"]

  # ==========================
  # UI
  # ==========================
  ui:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile.ui
      args:
        VITE_API_KEY: ${API_KEY}
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-/api}
    restart: unless-stopped
    profiles: ["ui"]
    ports:
      - "${UI_PORT:-5173}:80"

  # ==========================
  # Admin
  # ==========================
  pgadmin:
    image: dpage/pgadmin4:8
    restart: unless-stopped
    profiles: ["admin"]
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL:-admin@example.com}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:-admin}
      PGADMIN_CONFIG_SERVER_MODE: "False"
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${PGADMIN_PORT:-5050}:80"

  # ==========================
  # Observability (docker server)
  # ==========================
  prometheus:
    image: prom/prometheus:v2.54.1
    restart: unless-stopped
    profiles: ["obs"]
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
    volumes:
      - ../observability/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "${PROM_PORT:-9090}:9090"

  grafana:
    image: grafana/grafana:11.1.0
    restart: unless-stopped
    profiles: ["obs", "obs-host"]
    volumes:
      - grafana_data:/var/lib/grafana
      - ../observability/grafana/grafana/provisioning:/etc/grafana/provisioning
      - ../observability/grafana/grafana/dashboards:/var/lib/grafana/dashboards
    ports:
      - "${GRAFANA_PORT:-3000}:3000"

  prometheus_host:
    image: prom/prometheus:v2.54.1
    restart: unless-stopped
    profiles: ["obs-host"]
    command:
      - "--config.file=/etc/prometheus/prometheus.host.yml"
    volumes:
      - ../observability/prometheus/prometheus.host.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "${PROM_HOST_PORT:-9091}:9090"

  # ==========================
  # Eval
  # ==========================
  eval:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile.eval
    restart: "no"
    profiles: ["eval"]
    environment:
      # NOTE: server service name is "server"
      API_BASE_URL: ${API_BASE_URL:-http://server:8000}
      EVAL_ARGS: ${EVAL_ARGS:-}

      # Canonical pointer writing (eval_out)
      EVAL_WRITE_LATEST: ${EVAL_WRITE_LATEST:-1}
      # If your eval io supports EVAL_OUT_PATH (single file override), set it here.
      # Otherwise it will write eval_out/<task>/latest.json via llm_contracts default.
      EVAL_OUT_PATH: ${EVAL_OUT_PATH:-}
    volumes:
      # eval image runs in /work; it needs RW access to results + eval_out
      - ../..:/work:ro
      - ../../results:/work/results:rw
      - ../../eval_out:/work/eval_out:rw

  eval_host:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile.eval
    restart: "no"
    profiles: ["eval-host"]
    environment:
      API_BASE_URL: ${API_BASE_URL:-http://host.docker.internal:${API_PORT:-8000}}
      EVAL_ARGS: ${EVAL_ARGS:-}
      EVAL_WRITE_LATEST: ${EVAL_WRITE_LATEST:-1}
      EVAL_OUT_PATH: ${EVAL_OUT_PATH:-}
    volumes:
      - ../..:/work:ro
      - ../../results:/work/results:rw
      - ../../eval_out:/work/eval_out:rw

  # ==========================
  # Policy
  # ==========================
  policy:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile.policy
    restart: "no"
    profiles: ["policy"]
    environment:
      # Phase 0/1: prefer canonical eval pointer by default
      # With the updated policy CLI, "latest" means: follow eval_out pointer.
      POLICY_RUN_DIR: ${POLICY_RUN_DIR:-latest}

      POLICY_MODEL_ID: ${POLICY_MODEL_ID:-default}

      # Policy reads/writes models.yaml in the repo view by default
      POLICY_MODELS_YAML: ${POLICY_MODELS_YAML:-/work/config/models.yaml}

      # Thresholds packaged into the image, but you can override to point at repo-mounted thresholds if desired
      POLICY_THRESHOLDS_ROOT: ${POLICY_THRESHOLDS_ROOT:-/app/policy/src/llm_policy/thresholds}
      POLICY_THRESHOLD_PROFILE: ${POLICY_THRESHOLD_PROFILE:-extract/default}

      # Decision artifact path (canonical inside server container); policy writes to host bind-mount
      POLICY_OUT_PATH: ${POLICY_OUT_PATH:-/work/policy_out/latest.json}

      # Optional: allow policy to locate eval_out pointer directory explicitly
      # (only used if your policy loader reads env; safe to include)
      EVAL_OUT_PATH: ${EVAL_OUT_PATH:-}

      # Extra args escape hatch
      POLICY_ARGS: ${POLICY_ARGS:-}
    volumes:
      # Policy needs RW access to results + eval_out + policy_out + config
      - ../..:/work:rw
      - ../../results:/work/results:rw
      - ../../config:/work/config:rw
      - ../../eval_out:/work/eval_out:rw
      - ../../policy_out:/work/policy_out:rw
    working_dir: /work
    entrypoint: ["/bin/sh", "-lc"]
    command: >
      llm-policy decide-and-patch
      --run-dir "${POLICY_RUN_DIR}"
      --models-yaml "${POLICY_MODELS_YAML}"
      --model-id "${POLICY_MODEL_ID}"
      --threshold-profile "${POLICY_THRESHOLD_PROFILE}"
      --thresholds-root "${POLICY_THRESHOLDS_ROOT}"
      --artifact-out "${POLICY_OUT_PATH}"
      ${POLICY_ARGS}

volumes:
  pg_data:
  redis_data:
  pg_data_host:
  redis_data_host:
  grafana_data: